import { SellingTransactionMasterData } from './selling-transaction-master-data.model';

describe('SellingTransactionMasterData', () => {
  it('should create an instance', () => {
    expect(new SellingTransactionMasterData()).toBeTruthy();
  });
});
