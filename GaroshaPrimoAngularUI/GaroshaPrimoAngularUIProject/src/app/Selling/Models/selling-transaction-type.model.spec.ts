import { SellingTransactionType } from './selling-transaction-type.model';

describe('SellingTransactionType', () => {
  it('should create an instance', () => {
    expect(new SellingTransactionType()).toBeTruthy();
  });
});
